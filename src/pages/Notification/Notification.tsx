import React from 'react';
import { GetAllNotifications } from '../../components/Notification/GetAllNotification';

const Notification: React.FC = () => {
    return (
        <GetAllNotifications />
    );
};

export default Notification;
